/** Flip to false to turn lookups back on. Nothing else needs to change. */
export const SERVICE_PAUSED = true;

export const PAUSED_MESSAGE =
  "Sorry. Load is too high, so lookups are temporarily down. We’ll be back.";
